# AGENTS.md

Read `CLAUDE.md` in this directory — despite the name it is the session
bootstrap for **all** agents (codex included), not just Claude. It contains the
read order, the locked guardrails (O1/O3/O8, anti-trap build order), the
operating standard, the delegation method, and the known traps. Do not start
non-trivial work without it.
